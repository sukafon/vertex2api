# 安全策略

## 支持范围

安全修复只面向默认分支的最新版本。部署者应使用 `go.mod` 指定的 Go 补丁版本或更新的安全补丁，并在发布前运行 `govulncheck`。

## 报告漏洞

仓库公开后，请优先使用 GitHub 的 “Report a vulnerability” 私密安全公告提交问题。若该功能尚未启用，请联系维护者建立私密沟通渠道，不要在公开 Issue 中披露可利用细节、有效凭据或用户数据。

报告最好包含受影响版本、复现条件、影响、最小复现和建议修复。请先撤销误提交的真实 API 密钥、代理凭据或环境文件，再报告泄露。

## 边界说明

源码中的 GraphQL 查询签名和 reCAPTCHA 站点参数来自公开客户端流量，本身不作为服务器密钥。`GRAPHQL_API_KEY` 与 `RECAPTCHA_KEY` 现在通过环境变量提供，属于公开浏览器链路标识，不应与本项目的 `API_KEY`、`STATS_KEY` 或代理凭据混淆。它们仍可能被上游轮换，也不代表 Google 对本项目的认可。真实的 `API_KEY`、`STATS_KEY`、代理凭据、请求内容和生成内容不得提交到仓库或问题跟踪器。

服务端日志会保留经过长度限制的上游错误详情以便诊断。生产环境可设置 `REDACT_UPSTREAM_RESPONSES=true`，只隐藏返回给下游调用方的上游错误详情，不影响服务端日志。代理 URL 不会写入日志，避免泄露代理用户名或密码。
